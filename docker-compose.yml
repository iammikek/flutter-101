version: "3.9"
services:
  flutter_web:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - .:/app
      - flutter_pub_cache:/root/.pub-cache
    stdin_open: true
    tty: true
volumes:
  flutter_pub_cache: {}
